import { BicycleService, WeatherService } from '../../application';

export class BicycleWeatherApi {

    getWeatherOf(town) {
        if (!town) {
            return {};
        }
        return undefined;
    }
}